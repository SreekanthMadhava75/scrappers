name: Daily Horoscope Scraper

on:
  schedule:
    - cron: "30 2 * * *"   # Every day 2:30 AM IST
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run daily scraper
        env:
          AWS_ACCESS_KEY_ID: ${{ AKIA34YG24BA4OWIKDFL }}
          AWS_SECRET_ACCESS_KEY: ${{ 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 }}
          AWS_DEFAULT_REGION: ${{ Europe (Stockholm) eu-north-1 }}
        run: |
          python daily_scraper.py
